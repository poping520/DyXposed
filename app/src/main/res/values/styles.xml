<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="DyXposedCardView" parent="CardView">
        <item name="android:layout_marginStart">@dimen/cardview_margin_start</item>
        <item name="android:layout_marginEnd">@dimen/cardview_margin_start</item>
        <item name="android:layout_marginTop">@dimen/cardview_margin_top</item>
        <item name="android:layout_marginBottom">@dimen/cardview_margin_top</item>
    </style>

    <style name="DyXposedCardView.Material">
        <item name="android:clickable">true</item>
        <item name="android:foreground">?android:selectableItemBackground</item>
        <item name="android:stateListAnimator">@animator/anim_pressed_float</item>
    </style>

    <style name="DyXposedCardView.Material.Light">
        <item name="cardBackgroundColor">@color/light</item>
    </style>

</resources>
